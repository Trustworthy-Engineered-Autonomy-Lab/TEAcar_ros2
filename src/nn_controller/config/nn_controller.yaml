/nn_controller_node:
  ros__parameters:
    backend: "tensorflow"
    model_file: "/tmp/dummy.onnx"
    image_topic: "/camera/image_raw"
    control_rate_hz: 50.0
    steer_ratio: 1.0
    throttle_ratio: 1.0
    roi.x: 0
    roi.y: 0
    roi.width: 224
    roi.height: 144
    input_name: "input"
    output_name: "outputs"
